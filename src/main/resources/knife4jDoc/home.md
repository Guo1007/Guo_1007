# 家具商城 · API 接口文档

> **版本**：v1.1.0　|　**作者**：郭名城　|　**文档类型**：在线接口文档（Knife4j）

---

## 一、系统简介

家具商城是一个基于 **Spring Boot 3 + Vue 3** 的前后端分离在线家具销售平台，品牌定位为「顾家家居」。系统支持用户浏览家具、加购下单、收藏商品、评价商品、申请退款售后、收发站内通知，并提供 AI 智能客服「小智」在线答疑。

- **前台**：家具浏览、搜索、购物车、下单、订单管理、评价、退款售后、AI 客服
- **后台**：数据概览、用户/商品/分类/订单管理、售后处理、评价审核、通知管理、首页内容配置

---

## 二、功能模块

| 模块 | 说明 | 主要接口前缀 |
|------|------|--------------|
| 用户中心 | 注册/登录（验证码+密码）、密码找回、个人资料、收货地址 | `/user`、`/address` |
| 商品中心 | 分类浏览、商品列表/详情、SKU 规格、品牌筛选、相关推荐 | `/furniture`、`/furniture_type` |
| 购物车 | 本地购物车（支持游客加购，结算需登录） | 前端本地存储 |
| 订单中心 | 创建订单、支付、取消、发货、确认收货、超时自动取消 | `/order` |
| 退款售后 | 用户申请退款、管理员同意/拒绝/审核、退款释放库存 | `/order/refund`、`/admin/order/refund` |
| 收藏 | 商品收藏与取消 | `/favorite` |
| 评价系统 | 主评价、追评、评价评论区（支持回复）、审核机制 | `/comment`、`/review-comment` |
| 通知中心 | 站内通知、未读统计 | `/notification` |
| 站点内容 | 首页轮播图、服务栏等运营位配置 | `/site-content` |
| AI 客服 | 智能导购对话（流式响应，RAG 知识库） | `/ai/chat/stream` |
| 后台管理 | 数据看板、用户/商品/订单/售后/审核等 | `/admin/**` |

---

## 三、技术栈

| 分类 | 技术 |
|------|------|
| 后端框架 | Spring Boot 3、Spring Security、springdoc-openapi |
| ORM | MyBatis-Plus |
| 缓存/中间件 | Redis、Redisson（分布式锁） |
| 消息队列 | RocketMQ（订单邮件、评论回复通知） |
| 对象存储 | 阿里云 OSS（图片/视频上传） |
| AI | LangChain4j + OpenAI 兼容模型 + RAG |
| 接口文档 | Knife4j 4.x + springdoc 2.x |
| 前端 | Vue 3、Element Plus、Pinia |

---

## 四、接口约定

### 4.1 统一响应结构

所有接口返回统一的 `Result` 结构：

```json
{
  "code": 200,
  "success": true,
  "msg": "操作成功",
  "data": {}
}
```

| 字段 | 说明 |
|------|------|
| `code` | 状态码：200 成功，401 未登录，403 无权限，500 服务异常 |
| `success` | 是否成功 |
| `msg` | 提示信息 |
| `data` | 业务数据（对象或列表） |

### 4.2 认证方式

采用 **JWT Bearer Token** 认证，请求头携带：

```
Authorization: Bearer <token>
```

- 登录/注册接口返回 token，前端存入 `localStorage`
- 游客可访问：商品浏览、评论浏览、站点内容、购物车（本地）
- 需登录：下单、收藏、评价、收货地址、通知、AI 客服
- 管理端：`/admin/**` 需管理员角色（`is_admin = 1`）

### 4.3 订单状态机

| 状态码 | 含义 |
|--------|------|
| 0 | 待支付 |
| 1 | 已支付（待发货） |
| 2 | 已发货 |
| 3 | 已完成（待评价） |
| 4 | 已取消 |
| 5 | 已评价 |
| 6 | 申请退款中 |
| 7 | 退款审核中 |
| 8 | 已退款 |

---

## 五、快速开始

### 5.1 环境依赖

- JDK 17+
- MySQL 8.x
- Redis
- RocketMQ
- 阿里云 OSS（可选，用于文件上传）

### 5.2 常用调试流程

1. 调用 `/user/register` 注册账号，或 `/user/login` 登录获取 token
2. 复制返回的 token，在 Knife4j 右上角「Authorize」中填入 `Bearer <token>`
3. 即可调试购物、下单、评价等需登录接口
4. 管理员账号（`is_admin=1`）可访问 `/admin/**` 后台接口

### 5.3 分页参数

列表接口统一使用 `current`（页码，默认1）、`size`（每页条数，默认10）分页，返回结构含 `records`、`total`、`current`、`size`。

---

## 六、版本记录

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.1.0 | 2026-08-06 | 自定义 Knife4j 首页文档；完成退款售后、游客浏览、AI 客服等功能 |
| v1.0.0 | 2026-08-06 | 完成评价/追评/评论等核心功能 |
